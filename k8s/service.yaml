apiVersion: v1
kind: Service
metadata:
  name: markets
  namespace: lux-gateway
  labels:
    app: markets
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: markets
